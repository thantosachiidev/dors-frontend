# Dors Frontend

Static frontend for Dors, deployed with GitHub Pages.

The API remains on Render at the URL configured in `api-config.js`.

## Local build

```bash
npm install
npm run build
```

The GitHub Pages workflow publishes the `dist` directory from the `main` branch.
